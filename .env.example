DATABASE_URL=postgresql://...

# Supabase Configuration
# Lấy ở Project Settings -> API -> Project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Lấy ở Project Settings -> API -> Project API keys (anon, public)
SUPABASE_ANON_KEY=eyJh...

# Lấy ở Project Settings -> API -> Project API keys (service_role, secret)
# Key này cực kỳ quan trọng, dùng để backend có quyền admin (ví dụ: xoá user bị lỗi)
SUPABASE_SERVICE_ROLE_KEY=eyJh...

# Lấy ở Project Settings -> API -> JWT Settings -> JWT Secret
SUPABASE_JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long\

WEBHOOK_SECRET=your-webhook-secret